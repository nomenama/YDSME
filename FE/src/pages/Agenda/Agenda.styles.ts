import styled from "styled-components";
import {SecondaryButton} from "../Login/Login.styles";

export const UploadButton = styled(SecondaryButton)`
  width: 100%;
  align-self: center;
  margin: 0 auto 20px;
`